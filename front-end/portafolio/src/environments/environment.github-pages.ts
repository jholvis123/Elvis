export const environment = {
  production: true,
  // Placeholder until LIDER/user sets GitHub Actions variable API_URL (full origin + /api/v1).
  // Do not invent a production domain.
  apiUrl: 'https://YOUR-API-HOST/api/v1'
};
